__pycache__/
*.py[cod]
.venv/
venv/
reports/
.pytest_cache/

# Training-loop artifacts: metrics, fitted thresholds, mined examples.
# Anchored, so it never matches the auditor/training/ package.
/training/

# Bulk-ingestion output: regenerate with `python -m auditor --bulk ... --inventory-out`.
/inventory.json

# SQLite local knowledge database
auditor/rules/knowledge.db
