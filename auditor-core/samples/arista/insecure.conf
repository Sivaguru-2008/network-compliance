! device: sw-branch-02 (DCS-7010T-48, EOS-4.21.3F)
!
hostname sw-branch-02
!
enable password cleartext123
!
management ssh
   idle-timeout 0
   no shutdown
!
management telnet
   no shutdown
!
management api http-commands
   no shutdown
   protocol http
   protocol https
!
snmp-server community public ro
snmp-server community private rw
!
interface Ethernet1
   no shutdown
!
end
