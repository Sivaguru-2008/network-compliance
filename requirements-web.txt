# Optional: enables the web dashboard (`uvicorn auditor.web.app:app`).
# The deterministic core, the CLI, and PDF/JSON output all work without this;
# the web layer imports the core, and the core never imports the web layer.
-r requirements.txt
-r requirements-pdf.txt
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Read side only: tests/test_web.py drives the API with FastAPI's TestClient.
httpx>=0.27
