# SYNTHETIC FIXTURE — constructed from verified Stormshield Network Security CLI documentation.
# NOT a genuine device export. SNS version: 4.8.x.
# This configuration has partial/missing settings to test absence semantics and NEEDS_REVIEW.

# Stormshield Network Security v4.8.0
CONFIG HOSTNAME name="SNS-AMBIGUOUS-03"

CONFIG CONSOLE SSH state=1 port=22

CONFIG WEBADMIN state=1 allowhttp=0

CONFIG SNMP state=1
