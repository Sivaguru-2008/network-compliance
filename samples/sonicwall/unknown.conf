# SYNTHETIC FIXTURE — constructed from verified SonicWall E-CLI documentation.
# NOT a genuine device export. Version is unclear/unrecognised.

firmware-version SonicOS unknown

hostname SWFW-UNKNOWN

end
