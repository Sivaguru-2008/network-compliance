# SYNTHETIC FIXTURE — constructed from verified SonicWall E-CLI documentation.
# NOT a genuine device export. Contains deliberately malformed syntax.

firmware-version SonicOS

hostname

administration
  web-management https-port notanumber
  admin idle-timeout -5
  password-min-length abc
  exit

interface
  management
  exit

snmp
  snmp community-name
  exit

log
  syslog-server
  exit

ntp
  ntp-server
  exit

end
